# GAS再デプロイ手順（BEAT THE MIX 予約システム）

## 前回デプロイからの変更点（2026-07-08更新）
- **★FUTURE定期レッスン予約機能**: 定期レッスンに「定員」列（H列・空欄/0=無制限）を追加し、会員ページから予約可能に
- **★登録スタジオ生✅優先予約**: 予約時に「このスタジオに登録している生徒です」チェック→予約一覧の「登録スタジオ」列（H列）に✅記録。**満員時は✅のみ優先予約可**（✅なしは満員で予約不可）。✅の定員超過予約時は管理者メールに「⚠定員超過優先受付」と表示
- **★setupSheetsヘッダー更新**: 定期レッスンH1「定員」、予約一覧H1「登録スタジオ」
- ★再デプロイ後、スプレッドシートに手動でヘッダー追加（定期レッスンH1=定員、予約一覧H1=登録スタジオ）またはsetupSheets()再実行（データは消えない）

## 過去の変更点（2026-04-01）
- **お知らせ編集機能追加**: `editAnnouncement` POSTエンドポイント（タイトル/内容/重要タグを後から変更可能）
- **定期レッスン隔週対応**: 「頻度」「基準日」列を定期レッスンシートに追加。appendRowに2列追加

---

## 再デプロイ手順

### Step 1: Apps Scriptエディタを開く
1. BTMのスプレッドシートを開く
2. メニュー「拡張機能」→「Apps Script」

### Step 2: コードを全置換
1. エディタの既存コードを **全選択（Ctrl+A）→ 削除**
2. 下記「最新コード」セクションの内容を **全てコピー → 貼り付け**
3. **Ctrl+S** で保存

### Step 3: 再デプロイ
1. 右上「デプロイ」→「デプロイを管理」
2. 既存デプロイの右側 **鉛筆アイコン（編集）** をクリック
3. バージョンを **「新しいバージョン」** に変更
4. 「デプロイ」をクリック
5. 表示されたウェブアプリURLを確認

### Step 4: URL確認
- URLが前回と **同じ** → 何もしなくてOK
- URLが **変わった** → `members-page.html` の `GAS_URL` 定数を新URLに更新

---

## 動作確認チェックリスト
- [ ] 会員ページでスケジュールタブが表示される
- [ ] 定期レッスンがカレンダーに表示される（「定期レッスン」シートにデータ入力済みの場合）
- [ ] テスト予約 → スプレッドシートに記録される
- [ ] テスト予約 → beat.the.mix7386@gmail.com にメール通知が届く
- [ ] テスト予約（メールあり）→ 予約者に確認メールが届く
- [ ] 確認メールに「返信不可」「キャンセルはLINE」の文言がある
- [ ] 予約一覧タブで予約が表示される
- [ ] 管理者モードでレッスン枠追加/削除/編集ができる
- [ ] ★お知らせ追加時に「画像を添付」チェック → 写真選択 → プレビュー表示
- [ ] ★画像付きお知らせが追加後、管理画面でサムネイル表示される
- [ ] ★会員ページのお知らせに画像が表示される（タップで拡大）
- [ ] ★画像付きお知らせを削除 → Google Drive からも画像が消える
- [ ] ★ブラウザのDevTools→Networkタブで、管理者操作がPOSTで送信されていること確認
- [ ] ★URLにadminKeyが含まれていないこと確認
- [ ] ★定期休み追加 → カレンダーにグレーアウト表示
- [ ] ★定期休み削除 → グレーアウト解除
- [ ] ★お知らせピン留め → 一番上に固定表示
- [ ] ★ピン解除 → 通常の日付順に戻る
- [ ] ★管理者パスワード `potofu7386` でログインできる
- [ ] ★会員パスワード `btm2026` でログインできる
- [ ] ★お知らせ「✎ 編集」ボタン → タイトル/内容/重要タグを変更して保存できる
- [ ] ★定期レッスン追加で「隔週」切替 → 基準日入力欄が表示される
- [ ] ★FUTURE定期レッスンカードに「予約する」ボタン+残り枠が表示される
- [ ] ★予約モーダルの「登録スタジオの生徒です」✅ → 予約一覧H列に✅記録
- [ ] ★満員の定期レッスン: ✅なしは予約不可 / ✅ありは予約成功+管理者メールに「⚠定員超過優先受付」

---

## 最新コード（booking-script.gs）

以下のいずれかの方法で最新コードを取得してApps Scriptエディタに貼り付ける:

**方法1: GitHub Raw URL**（推奨）
https://raw.githubusercontent.com/isai24839a-tech/btm-design/master/booking-script.gs
→ ブラウザで開いて Ctrl+A → Ctrl+C

**方法2: ローカルファイル**
`C:\Users\shirasaki910\Desktop\dance-studio\booking-script.gs` をテキストエディタで開いてコピー

**⚠️ この手順書へのコード埋め込みは廃止（2026-07-08）**: 過去に埋め込んだコードが更新されずに古いまま残り、「最新版」と誤表記される事故があったため。必ず上記の方法1（GitHub Raw）か方法2（ローカルファイル）= `booking-script.gs` 本体からコピーすること。
