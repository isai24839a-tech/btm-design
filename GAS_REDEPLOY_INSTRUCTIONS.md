# GAS再デプロイ手順（BEAT THE MIX 予約システム）

## 前回デプロイからの変更点（2026-07-14夜・第2弾）
- **★メール送信元をBEAT THE MIX名義に**: 全メール（予約通知・確認・キャンセル・キャンセル待ち）の送信元表示を「BEAT THE MIX」に変更。さらに **isai24839aのGmailで beat.the.mix7386@gmail.com を「他のメールアドレスから送信」エイリアス登録済みなら、送信元アドレス自体が beat.the.mix7386@gmail.com になる**（個人アドレスが生徒に見えなくなる・返信もbeat側へ）。エイリアス未登録の間も送信は失敗せず、表示名だけ変わる安全設計（`mailOptions()`が毎回`getAliases()`で判定）

## 過去の変更点（2026-07-14・デプロイ済み）
- **★FUTURE予約通知の停止**: FUTUREレッスンの「新しい予約が入りました」管理者通知メールを完全停止（beat.the.mix7386@gmail.com / isai24839a@gmail.com とも送らない）。**KIDS予約の通知は従来どおり両方に届く**。キャンセル通知（【BTMキャンセル】）と予約者宛の確認メールは変更なし

## 過去の変更点（2026-07-13・デプロイ済み）
- **★FUTURE予約確認メールにキャンセル方法を明記**: FUTUREレッスンの予約確認メールに「■ キャンセル方法」（会員ページ→予約者一覧→自分のニックネーム横の「キャンセル」ボタン・会員ページURL付き・予約時と同じ端末のみ操作可の注記・できない場合はLINE）を記載。KIDSの確認メールは従来どおり「キャンセル・変更はLINEで」のまま
- **★予約シートのKIDS/FUTURE分割**: 予約の記録先を「予約一覧」1シート混在 → 「**予約一覧KIDS**」「**予約一覧FUTURE**」の2シートに分割。予約時にレッスンのカテゴリを判定して該当シートへ記録。**再デプロイ後の初回アクセスで、旧「予約一覧」の既存データは自動でKIDS/FUTUREに振り分け移行され、旧シートは削除される（手動作業不要）**。予約数カウント・満員判定・予約者一覧・キャンセル・キャンセル待ちはすべて両シート横断で動くので機能への影響なし。カテゴリ判定不能な古い行はKIDS側に入る（機能影響なし・見た目のみ）
- **★定期レッスンの期間設定（開催期間）**: 定期レッスン（毎週・隔週）に「開催期間」（開始日〜終了日・どちらか片方だけでもOK・空欄=ずっと開催）を設定可能に。期間外の日はカレンダーに表示されず、予約・満員判定・キャンセル待ちの対象にもならない。さらに予約受付（bookSlot）自体もサーバー側でブロック: スケジュール枠にも開催日の定期レッスンにも一致しない予約は「この日はこのレッスンの開催日ではないため予約できません」で拒否（旧コードは該当なしでもデフォルト定員10で受け付けてしまう穴があった）。定期レッスンシートにI列「期間開始」・J列「期間終了」が増えるが**列とヘッダーは自動作成されるので手動作業不要**。管理者モードの追加フォームと編集フォーム（✎）の両方に期間入力欄あり
- **★臨時定員（指定日だけ定員変更）**: 管理者モードのFUTURE定期レッスンカードに「👥 この日の定員」ボタン → その日だけの定員を入力（0=無制限）。「臨時定員」シート（無ければ自動作成・手動作業不要）に記録され、**その日だけ**通常の定員より優先される。残り枠表示・満員判定・✅優先予約・キャンセル待ちすべてに反映。「解除」ボタンで通常の定員に戻る
- **★キャンセル待ち機能（FUTURE）**: 満員のFUTUREレッスンに「🔔 キャンセル待ち登録」ボタン → ニックネーム+メール（必須）で GET `action=waitlist` 登録。「キャンセル待ち」シート（無ければ自動作成・setupSheets再実行不要）に記録+受付メール送信。**本人/管理者どちらのキャンセルでも空きが出たら**、そのレッスンのキャンセル待ち全員に「空きが出ました」メール（先着順・自動予約なし）を送って登録解除。過去日レッスン・まだ満員（✅優先の定員超過中）の場合は通知しない。空きがある状態での待ち登録は拒否
- **★予約者本人キャンセル機能**: 会員ページのFUTURE定期レッスン予約者一覧に、本人（localStorageの予約名と一致）だけ「キャンセル」ボタン表示 → POST `memberCancelBooking`（adminKey不要）で予約行を削除。過去日はキャンセル不可。キャンセル時に管理者へ通知メール＋予約者へ確認メール（メール登録時のみ）
- **★FUTURE予約は「同じレッスン1人1枠」制限**（2026-07-13 ルール変更: 全体1枠→同一レッスン1枠）: FUTUREレッスンの予約時、同じ名前で「同じ日・同じレッスン」の予約が既にあればサーバー側で拒否（「同じレッスンのご予約はお一人1枠までです」）。**別の日・別のレッスンは予約可**。members-page.html側にも同じチェックを送信前に入れてあるが、本命はGAS側（再デプロイ必須）
- 内部変更: `findRegularLessonCapacity` を `getRegularLessonData` + `matchRegularLesson` に分割（定員に加えてカテゴリ列も参照するため）
- シート構成: 「キャンセル待ち」シートが増えるが**自動作成されるので手動作業不要**（再デプロイのみでOK）

## 過去の変更点（2026-07-08）
- **★FUTURE定期レッスン予約機能**: 定期レッスンに「定員」列（H列・空欄/0=無制限）を追加し、会員ページから予約可能に
- **★登録スタジオ生✅優先予約**: 予約時に「このスタジオに登録している生徒です」チェック→予約一覧の「登録スタジオ」列（H列）に✅記録。**満員時は✅のみ優先予約可**（✅なしは満員で予約不可）。✅の定員超過予約時は管理者メールに「⚠定員超過優先受付」と表示
- **★setupSheetsヘッダー更新**: 定期レッスンH1「定員」、予約一覧H1「登録スタジオ」
- ★再デプロイ後、スプレッドシートに手動でヘッダー追加（定期レッスンH1=定員、予約一覧H1=登録スタジオ）またはsetupSheets()再実行（データは消えない）

## 過去の変更点（2026-04-01）
- **お知らせ編集機能追加**: `editAnnouncement` POSTエンドポイント（タイトル/内容/重要タグを後から変更可能）
- **定期レッスン隔週対応**: 「頻度」「基準日」列を定期レッスンシートに追加。appendRowに2列追加

---

## 再デプロイ手順

### Step 1: Apps Scriptエディタを開く
1. BTMのスプレッドシートを開く
2. メニュー「拡張機能」→「Apps Script」

### Step 2: コードを全置換
1. エディタの既存コードを **全選択（Ctrl+A）→ 削除**
2. 下記「最新コード」セクションの内容を **全てコピー → 貼り付け**
3. **Ctrl+S** で保存

### Step 3: 再デプロイ
1. 右上「デプロイ」→「デプロイを管理」
2. 既存デプロイの右側 **鉛筆アイコン（編集）** をクリック
3. バージョンを **「新しいバージョン」** に変更
4. 「デプロイ」をクリック
5. 表示されたウェブアプリURLを確認

### Step 4: URL確認
- URLが前回と **同じ** → 何もしなくてOK
- URLが **変わった** → `members-page.html` の `GAS_URL` 定数を新URLに更新

---

## 動作確認チェックリスト
- [ ] 会員ページでスケジュールタブが表示される
- [ ] 定期レッスンがカレンダーに表示される（「定期レッスン」シートにデータ入力済みの場合）
- [ ] テスト予約 → スプレッドシートに記録される
- [ ] KIDSテスト予約 → beat.the.mix7386@gmail.com にメール通知が届く
- [ ] ★FUTUREテスト予約 → 管理者への通知メールはどこにも届かない（予約者宛の確認メールのみ）
- [ ] テスト予約（メールあり）→ 予約者に確認メールが届く
- [ ] KIDS予約の確認メールに「返信不可」「キャンセルはLINE」の文言がある
- [ ] ★FUTURE予約の確認メールに「■ キャンセル方法」（会員ページの手順+URL+同一端末の注記）がある
- [ ] 予約一覧タブで予約が表示される
- [ ] 管理者モードでレッスン枠追加/削除/編集ができる
- [ ] ★お知らせ追加時に「画像を添付」チェック → 写真選択 → プレビュー表示
- [ ] ★画像付きお知らせが追加後、管理画面でサムネイル表示される
- [ ] ★会員ページのお知らせに画像が表示される（タップで拡大）
- [ ] ★画像付きお知らせを削除 → Google Drive からも画像が消える
- [ ] ★ブラウザのDevTools→Networkタブで、管理者操作がPOSTで送信されていること確認
- [ ] ★URLにadminKeyが含まれていないこと確認
- [ ] ★定期休み追加 → カレンダーにグレーアウト表示
- [ ] ★定期休み削除 → グレーアウト解除
- [ ] ★お知らせピン留め → 一番上に固定表示
- [ ] ★ピン解除 → 通常の日付順に戻る
- [ ] ★管理者パスワード `potofu7386` でログインできる
- [ ] ★会員パスワード `btm2026` でログインできる
- [ ] ★お知らせ「✎ 編集」ボタン → タイトル/内容/重要タグを変更して保存できる
- [ ] ★定期レッスン追加で「隔週」切替 → 基準日入力欄が表示される
- [ ] ★FUTURE定期レッスンカードに「予約する」ボタン+残り枠が表示される
- [ ] ★予約モーダルの「登録スタジオの生徒です」✅ → 予約一覧H列に✅記録
- [ ] ★満員の定期レッスン: ✅なしは予約不可 / ✅ありは予約成功+管理者メールに「⚠定員超過優先受付」
- [ ] ★自分が予約したFUTURE定期レッスンの予約者一覧に「キャンセル」ボタンが出る（他人の予約には出ない）
- [ ] ★キャンセル実行 → スプレッドシートから行削除+管理者に「【BTMキャンセル】」メール
- [ ] ★FUTURE予約済みと同じレッスンを同じ名前でもう一度予約 → 「同じレッスンのご予約はお一人1枠までです」エラーで拒否される
- [ ] ★FUTURE予約済みでも別の日・別のレッスンは予約できる
- [ ] ★既存のFUTURE予約をキャンセル後 → 同じレッスンを再予約できる
- [ ] ★KIDS側の予約は同一レッスン1枠制限の影響を受けない
- [ ] ★満員のFUTUREレッスンに「🔔 キャンセル待ち登録」ボタンが出る（空きがあるレッスンには出ない）
- [ ] ★キャンセル待ち登録 → 「キャンセル待ち」シートに記録+登録者に受付メール
- [ ] ★同じレッスンに同じニックネーム/メールで二重登録 → 「既に登録済み」エラー
- [ ] ★満員レッスンの予約を1件キャンセル → キャンセル待ち登録者に「空きが出ました」メール+シートから登録が消える
- [ ] ★KIDS側には キャンセル待ちボタンが出ない
- [ ] ★管理者モードのFUTURE定期レッスンカードに「👥 この日の定員」ボタンが出る（KIDSには出ない）
- [ ] ★「この日の定員」で定員設定 → 「臨時定員」シートに記録+その日だけ残り枠表示が変わる（他の日は通常定員のまま）
- [ ] ★臨時定員で満員になった日 → 予約ブロック+キャンセル待ちボタン表示（✅登録スタジオ生は優先予約可）
- [ ] ★臨時定員の「解除」ボタン → シートから行削除+通常の定員に戻る
- [ ] ★定期レッスン追加フォームに「開催期間」入力欄が出る（「単発」選択時は消える）
- [ ] ★期間を設定した定期レッスン → 期間外の日のカレンダーに表示されない（期間内は表示される）
- [ ] ★管理一覧カードに「📅 開始日〜終了日」タグが表示される
- [ ] ★編集（✎）で開催期間を後から設定/変更/空欄クリアできる
- [ ] ★再デプロイ後に会員ページを1回開く → スプシに「予約一覧KIDS」「予約一覧FUTURE」が自動作成され、旧「予約一覧」の予約が振り分けられて旧シートが消える
- [ ] ★KIDSレッスンをテスト予約 → 「予約一覧KIDS」に記録される
- [ ] ★FUTUREレッスンをテスト予約 → 「予約一覧FUTURE」に記録される
- [ ] ★分割後も予約者一覧・残り枠・満員判定・本人キャンセルが今まで通り動く

---

## 最新コード（booking-script.gs）

以下のいずれかの方法で最新コードを取得してApps Scriptエディタに貼り付ける:

**方法1: GitHub Raw URL**（推奨）
https://raw.githubusercontent.com/isai24839a-tech/btm-design/master/booking-script.gs
→ ブラウザで開いて Ctrl+A → Ctrl+C

**方法2: ローカルファイル**
`C:\Users\shirasaki910\Desktop\dance-studio\booking-script.gs` をテキストエディタで開いてコピー

**⚠️ この手順書へのコード埋め込みは廃止（2026-07-08）**: 過去に埋め込んだコードが更新されずに古いまま残り、「最新版」と誤表記される事故があったため。必ず上記の方法1（GitHub Raw）か方法2（ローカルファイル）= `booking-script.gs` 本体からコピーすること。
